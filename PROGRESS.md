# Progress

Status snapshot for picking up work on any machine. Update this at the end of each session.

## Current status
UI polish pass complete. Brand colour is now `#192841` (navy) throughout. App always renders in light mode regardless of device setting. Homepage example is now a phone-frame mockup (see log below) rather than the flat card. Parks/walks added as a fifth venue option across the occasion picker, venue filter buttons, and venues API. Everything below is committed and live on Vercel.

**Housekeeping TODO:** `.claude/worktrees/journey-detail-and-ranking/` is a leftover git worktree (with its own `.next` build cache) sitting untracked in the repo folder from an earlier session. It's harmless but unused — safe to delete, or add `.claude/` to `.gitignore` if it should stay for future worktree use.

## Build order checklist
- [x] Scaffold Next.js + TypeScript + Tailwind, set up folder structure
- [x] `/lib/candidates.ts` — hardcoded station list with lat/lng (zones 1–3, 30 stations, coordinates pulled live from TfL's StopPoint Search)
- [x] `/lib/tfl.ts` — `getJourney(fromLat, fromLng, toLat, toLng, timePreference?)` returning full legs + duration, concurrency-limited batch helper, KV-cached (6h TTL, time-preference-aware cache key, null results not cached), retries on 429
- [x] `/lib/lineColours.ts` — static TfL line-colour lookup (`colourForLine(lineName, mode)`) used to colour journey-leg dots on the Results screen
- [x] `/lib/geocode.ts` — `resolveLocation(input)` — postcode regex check, then Postcodes.io or TfL StopPoint Search — tested against the real APIs
- [x] `/lib/algorithm.ts` — `findBestStations(locations, timePreference?, count=3)` — minimax, returns top 3 ranked stations with per-person legs, time difference, average time
- [x] `/lib/session.ts` — KV-backed session store with `setTimePreference`, ranked `SessionResults` shape, `removeLocation`
- [x] `/lib/venues.ts` — `getNearbyVenues(lat, lng, radius)` — Google Places Nearby Search across restaurants/cafes/bars, KV-cached 12h
- [x] API routes — all original routes plus `POST /api/session/[id]/time` (set/clear arrive-by/depart-at), calculate now returns ranked stations, venues fetched on demand not upfront — 164 tests passing
- [x] UI screens — Home, redesigned Session (multi-row form + time preference toggle), Results (top 3 ranked station cards with coloured TfL legs, per-person map overlay, on-demand venues, meeting-area map that follows selected card)

## Log
- 2026-06-22 — Wrote `CLAUDE.md` project spec (algorithm, stack, API routes, data model).
- 2026-06-23 — Added this progress note, pushed repo to GitHub (tessturner12, public).
- 2026-06-23 — Filled spec gaps: TfL StopPoint Search for station/area name input, max-10 concurrency limit on TfL calls, KV caching for Google Places venue results. Build order now includes a `/lib/geocode.ts` step.
- 2026-06-23 — Repositioned v1 as solo-first: one person fills in up to 6 locations themselves, share link now optional rather than the primary flow. Location cap raised 4 → 6 throughout the spec. Real-time collaborative "everyone joins" flow stays in Phase 2.
- 2026-06-23 — Scaffolded Next.js 16 + TypeScript + Tailwind v4 + App Router. Verified `npm run build` and `npm run dev` both work, homepage loads. Folder structure (`/app/api`, `/components`, `/lib`, `/types`) created.
- 2026-06-23 — Added `/lib/candidates.ts` (30 Zone 1–3 stations, coordinates fetched live from TfL's StopPoint Search API rather than typed from memory, for accuracy). Added `/lib/tfl.ts` with `getJourneyTime()` and a concurrency-limited `getJourneyTimes()` batch helper, test-driven with Vitest (6 tests) and confirmed working against the real TfL API (no key needed yet, just rate-limited). Vercel KV not set up yet, so caching is a TODO left in the file.
- 2026-06-23 — Added `/lib/geocode.ts` (`resolveLocation()`, postcode vs station/area name routing) and `/lib/algorithm.ts` (`findBestStation()`, the minimax logic), both test-driven and confirmed against real APIs/scenarios. All four core lib modules are now done — 15 tests passing, `tsc` and `eslint` both clean. Next (API routes) needs a Vercel KV store, which doesn't exist yet.
- 2026-06-24 — Set up Vercel KV (Upstash marketplace integration) and saved env vars to `.env.local`. Added `/lib/kv.ts` (Redis client) and `/lib/session.ts` (session CRUD, 9 tests, test-driven). Built and test-drove 4 API routes: `POST /api/session`, `GET /api/session/[id]`, `POST /api/session/[id]/locate`, `POST /api/session/[id]/calculate` (13 tests). Confirmed the whole flow works for real — created a session, geocoded two real locations (a postcode and a station name), ran the calculation against live TfL data, got back a correct, fair result (Bank station, 31 min for both people) in ~12s. `npm test` (37 tests), `tsc`, and `eslint` all clean; production build succeeds. `GET /api/venues` still needs a Google Maps API key — next blocker, same shape as the KV one.
- 2026-06-24 — Set up a Google Cloud project, enabled Places API + Maps JavaScript API, created an API key, saved it to `.env.local` as both `GOOGLE_MAPS_API_KEY` and `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`. Added `/lib/venues.ts` (Google Places Nearby Search across restaurants/cafes/bars, KV-cached 12h, 5 tests, test-driven) and `GET /api/venues` (3 tests). Wired venue fetching into the calculate route so results now include real venues automatically (with graceful fallback to an empty list if Places fails). Confirmed for real: a fresh two-person session (postcode + station name) ran end-to-end and picked Angel station (29 min for both people), with real nearby restaurants/cafes/bars attached. `npm test` (46 tests), `tsc`, and `eslint` all clean. All planned API routes are now done — only the UI screens (Stage 6) remain.
- 2026-06-25 — Built all three Phase 1 MVP screens: Home (`app/page.tsx`), Session (`app/session/[id]/page.tsx` + `components/LocationForm.tsx`), Results (`app/session/[id]/results/page.tsx` + `components/RallyMap.tsx`), plus a shared Google Maps script loader (`lib/googleMaps.ts`). No new automated tests for the UI itself (decided to verify by hand rather than add jsdom/React Testing Library); each task was code-reviewed individually as it landed. End-to-end verification this round was done via `curl` against the live backend (no browser automation tool available in this environment): created a session, added two real locations (postcode `SW1A 1AA` and area name `Brixton`), ran `/calculate`, and got back a correct, sensible result (Victoria station, 12 and 21 min journey times, real nearby restaurants/cafes/bars attached) — then confirmed both `/session/[id]` and `/session/[id]/results` return HTTP 200. `npm test` (46 tests), `tsc`, and `eslint` all clean.
- 2026-06-25 — A real manual run (3 locations: Shepherd's Bush, Clerkenwell, Tooting Bec) hit "Could not find a station with a known journey time for everyone" — root cause was TfL's unauthenticated rate limit: with up to 90 journey lookups per search, enough came back `429 Too Many Requests` that every candidate station ended up missing a journey time for someone, even though most journeys were fine. Fixed `lib/tfl.ts` to retry on `429` (up to 3 attempts with backoff) instead of treating it the same as a genuine no-route response, test-driven (3 new tests). Also added a real `TFL_API_KEY` to `.env.local`, which raises the rate limit enough that the same scenario now resolves correctly (Oxford Circus, 32 min longest journey) — confirmed with 10 concurrent unauthenticated calls going from 0–4/10 succeeding to 10/10 once the key was added. `npm test` (49 tests), `tsc`, and `eslint` all clean.
- 2026-06-25 — Did the manual browser click-through that was outstanding from earlier: Home → Session (autocomplete, adding locations) → Results (map, journey times, venues) confirmed working in a real browser. Phase 1 MVP is now fully done, not just code-complete.
- 2026-06-25 — Wired up the KV caching for `getJourneyTime()` in `lib/tfl.ts` that was deferred back when Vercel KV didn't exist yet (cache key `tfl:{from}:{to}`, 6h TTL, test-driven with 3 new tests, mirroring the pattern already used in `lib/venues.ts`). Deliberately does not cache `null` results, since those mostly happen during transient TfL rate-limiting and caching one would lock in the failure for 6 hours. This was the one remaining gap from the original `CLAUDE.md` spec — Phase 1 is now fully spec-complete. `npm test` (53 tests), `tsc`, and `eslint` all clean.
- 2026-06-25 — Added the ability to remove a location after adding it on the Session screen (`lib/session.ts` `removeLocation()`, a new `DELETE /api/session/[id]/locate` route, and a "Remove" button per row in `app/session/[id]/page.tsx`). Removal is immediate, no confirmation step, by design. Test-driven (4 new tests in `lib/session.test.ts`, 5 new tests in the route's test file), and confirmed by hand in the browser: adding/removing locations, removing down to 0, removing to free up a slot at 6/6, and reloading to confirm the removal persisted server-side. `npm test`, `tsc`, and `eslint` all clean.
- 2026-06-25 — Merged journey-detail-and-ranking branch (13 tasks): full TfL journey legs with line colours and per-person map overlays; top-3 ranked station results instead of a single winner; redesigned Session screen with multi-row form and optional arrive-by/depart-at time toggle; `MeetingAreaMap` (follows selected ranked card), `StationCard` (per-person legs, fairness stats, on-demand venues), `PersonJourneyMap` (full-screen Google transit directions overlay). 164 tests passing. Note: `PersonJourneyMap` requires the Google Directions API to be enabled in Google Cloud Console before "Show map" will render a real route.
- 2026-06-26 — Three UI improvements: (1) replaced browser native time picker in `TimePreferenceToggle` with a 15-minute-interval dropdown (00:00–23:45); (2) added a live Google Map showing Oxford Circus to the homepage example section, matching the style of the real results screen; (3) rebranded all blue across every screen and component from Tailwind `blue-*` to `#02075d` (dark navy), including hardcoded circle colours in `MeetingAreaMap`. Also added TfL line-coloured dots with fake journey legs to the homepage example card, and made person names bold relative to the "from X" text. All pushed to GitHub, auto-deployed to Vercel.
- 2026-06-26 — Three UX refinements: (1) "What are you meeting up for?" occasion selector moved from the home page to the session page (page 2), shown as four pill buttons (Anything/Food/Drinks/Coffee) above the Find Rally Point button; occasion flows as `?for=` param to results and pre-selects the venue filter; (2) Venue filter buttons (All/Food/Coffee/Drinks) now appear only after clicking "Find Nearby Venues", not before — clicking a filter re-fetches for that type; (3) Each person's journey card now shows a static Google Maps thumbnail (origin → station) to the right of the leg list, with "Show route" below it opening the full interactive route.
- 2026-06-26 — Two final polish tweaks: (1) route preview strip (dot–bar–dot) moved to sit inline with the person's name rather than on its own row; (2) replaced the dynamic Google Static Maps thumbnail in each person card with a fixed decorative SVG (two crossing tube lines, small station circles) — no API call, purely decorative.
- 2026-07-07 — Rebuilt the homepage example as a phone-frame mockup: the flat card + map are now wrapped in a CSS iPhone shell (dark bezel, dynamic island, home bar) with an amber "Example" badge above it, holding a compact version of the station card. Pushed and deployed to production (`joinrally.place`). Follow-up round in the same session: widened the phone 268px → 320px; added `interactive` and `zoom` props to `MeetingAreaMap` so the homepage copy can be a static, non-interactive preview (no drag/scroll-zoom/controls) zoomed out a bit further than the real results-page map, which still defaults to fully interactive; changed the example/How-it-works grid from an even 2-column split to `auto` + `1fr` (phone column only takes the width it needs) with `md:pl-6` breathing room before the phone, since the phone reads tall and narrow rather than square. All changes committed, pushed, and deployed to production — `tsc` clean, no automated visual check (no browser tool in this environment), verified by the user in a local dev server before deploying.
- 2026-06-26 — Six further improvements: (1) Home page layout — example card on left, "How it works" on right, side-by-side grid on wider screens; (2) "What are you meeting up for?" occasion dropdown on the home screen (All / Food / Drinks / Coffee) — flows through as a `?for=` URL param into the session and results pages; (3) CTA button now has pointer cursor, drop shadow, and a subtle scale-up/scale-down hover/press effect; (4) Journey leg display — tube/DLR/Overground/Elizabeth line legs now show as a horizontal coloured bar rather than a dot (walk/bus remain dots); a compact route preview strip (bars + dots in sequence with `›` separators) appears above each person's detailed leg list; (5) "Show map" renamed to "Show route" throughout; (6) Each StationCard now has a "Share these results" button at the bottom, plus a venue-type filter dropdown (All / Food / Drinks / Coffee) next to "Find Nearby Venues" — changing the filter re-fetches venues for that type. Venues API extended to accept `?for=` param mapping to Google Places types; cache key updated to include types. 165 tests passing.
- 2026-06-29 — Four UI changes: (1) Brand colour updated from `#02075d` to `#192841` (navy) across all screens and components; (2) App locked to light mode — dark mode CSS removed and `colorScheme: light` set on html element, so device dark mode setting has no effect; (3) Homepage example now uses the real `MeetingAreaMap` component (full Google Map with station marker and walking-distance circle, matching the results page exactly) instead of a simplified inline map; (4) Parks/walks added as a fifth venue option — `🌳 Parks` pill in the session occasion picker and station card venue filters, backed by Google Places `park` type (`for=walks` param in venues API). Occasion picker and filter grids changed from fixed 4-col to flex-wrap to accommodate 5 options.
- 2026-06-29 — Fixed homepage example card layout to match real results page: route preview bars now sit inline with person name (not on a separate row), decorative tube-map SVG and greyed-out "Show route" button added to the right of each person's leg list.
